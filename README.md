# Luyện tập nhúng CSS

- `inline-style.html`: CSS bằng thuộc tính `style` ngay trên thẻ HTML.
- `internal-style.html`: CSS trong cặp thẻ `<style>` của tài liệu HTML.
- `external-style.html`: CSS liên kết từ tệp `style.css`.
